# Local git worktrees (isolated implementation workspaces)
.worktrees/

# Atlas agent tool state (session scaffolding, not project content)
.atlas/

# Brainstorming visual-companion scratch state (mockup previews, server pid/logs)
.superpowers/

# Node / build (added ahead of scaffolding the app)
node_modules/
dist/
.output/
.vercel/
.netlify/
*.local
.env
.env.local
.DS_Store
tests/e2e/artifacts/
test-results/
playwright-report/


